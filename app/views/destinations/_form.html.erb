<%= form_for([@route, @route.destinations.build]) do |f| %>
  <div class="field">
    <%= f.label :name %><br/>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    ID<br/>
    <%= f.text_field :destID %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>